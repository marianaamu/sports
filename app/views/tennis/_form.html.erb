<%= form_for(@tenni) do |f| %>
  <% if @tenni.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tenni.errors.count, "error") %> prohibited this tenni from being saved:</h2>

      <ul>
      <% @tenni.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
